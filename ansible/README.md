1. first build with the build script
2. Then enter the running container:
   ```bash
   docker run --rm -it nvim-computer bash
   ```
   then run script with `ansible-playbook fem.yml` or other file you wanna run
